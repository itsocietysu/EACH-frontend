/*
 * LabelFile Messages
 *
 * This contains all the text for the LabelFile component.
 */
import { defineMessages } from 'react-intl';

export default defineMessages({
  selectFile: {
    id: 'each.components.LabelFile.selectFile',
    defaultMessage: 'Select File',
  },
});
